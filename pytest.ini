[pytest]
testpaths = .
python_files = test_*.py
asyncio_mode = auto

addopts =
    --cov=app
    --cov-report=html
    --cov-report=xml
    --cov-report=term-missing
    --cov-fail-under=70
    -v

[coverage:run]
source = .
omit =
    test_*.py
    */__pycache__/*
    */venv/*
